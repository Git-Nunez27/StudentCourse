# 🔴 Error Handling Guide - วิธีแก้ไขปัญหา

## 📌 บทนำ

ไฟล์นี้อธิบายวิธีการแก้ไขปัญหาทั่วไปเมื่อทดสอบ API ด้วย Postman

---

## 🚨 Error Categories

### 1️⃣ Client Errors (4xx)

ข้อผิดพลาดจาก client ที่ส่ง request ไม่ถูกต้อง

#### ❌ 400 Bad Request

**ความหมาย:** Request body หรือ parameters ไม่ถูกต้อง

**ตัวอย่าง:**
```json
{
  "success": false,
  "message": "⚠️ ขาดข้อมูลที่จำเป็น: fullname, email, major"
}
```

**วิธีแก้:**
```
1. ✅ ตรวจสอบ body ข้อมูล
   - POST /api/students: ต้องมี fullname, email, major
   - POST /api/courses: ต้องมี name, description, credit
   - POST /api/enrollments: ต้องมี student_id, course_id

2. ✅ ตรวจสอบ Content-Type header
   Content-Type: application/json

3. ✅ ตรวจสอบ JSON format ถูกต้อง
   - ใช้ double quotes (")
   - ไม่มี trailing comma
   - Proper data types (string, number, boolean)
```

---

#### ❌ 404 Not Found

**ความหมาย:** ไม่พบ resource ที่ต้องการ

**ตัวอย่าง:**
```json
{
  "success": false,
  "message": "⚠️ ไม่พบนักเรียน"
}
```

**สาเหตุทั่วไป:**

| สาเหตุ | วิธีแก้ |
|------|--------|
| ID ไม่ถูกต้อง | ตรวจสอบ ID ใน environment variables |
| ไม่มี student/course ที่ ID นั้น | สร้าง student/course ใหม่ก่อน |
| ลบแล้ว | ไม่สามารถดึงข้อมูลที่ลบแล้ว |

**วิธีแก้:**
```bash
# 1. ตรวจสอบ student มีอยู่
GET {{base_url}}/students
# ✅ ดูว่ามี student ไหม

# 2. ตรวจสอบ ID ถูกต้อง
GET {{base_url}}/students/{{student_id}}
# ✅ ใช้ ID จากตัวแปร environment

# 3. สร้างข้อมูลใหม่ถ้าไม่มี
POST {{base_url}}/students
{
  "fullname": "สมชาย ใจดี",
  "email": "somchai@example.com",
  "major": "IT"
}
```

---

#### ❌ 409 Conflict

**ความหมาย:** ข้อมูลซ้ำกับที่มีอยู่ (Duplicate)

**ตัวอย่าง:**
```json
{
  "success": false,
  "message": "⚠️ นักเรียนลงทะเบียนวิชานี้แล้ว"
}
```

**สาเหตุทั่วไป:**

| สาเหตุ | วิธีแก้ |
|------|--------|
| Email ซ้ำ | ใช้ email ใหม่ที่ไม่ซ้ำ |
| ลงทะเบียนซ้ำ | ใช้ course_id ต่างนอก |
| Data ซ้ำกัน | ตรวจสอบข้อมูลก่อน |

**วิธีแก้:**
```bash
# 1. สำหรับ Email ซ้ำ
POST {{base_url}}/students
{
  "fullname": "คนใหม่",
  "email": "newemail@example.com",  # ✅ ใช้ email ใหม่
  "major": "IT"
}

# 2. สำหรับ Enrollment ซ้ำ
# ✅ ใช้ course_id ต่างนอก
POST {{base_url}}/enrollments
{
  "student_id": "existing-id",
  "course_id": "different-course-id"  # ✅ ต่างนอกจากที่ลงทะเบียนแล้ว
}
```

---

### 2️⃣ Server Errors (5xx)

ข้อผิดพลาดจาก server

#### ❌ 500 Internal Server Error

**ความหมาย:** Server เกิดข้อผิดพลาดภายใน

**ตัวอย่าง:**
```json
{
  "success": false,
  "message": "Internal server error",
  "error": "connect ECONNREFUSED 127.0.0.1:6432"
}
```

**สาเหตุทั่วไป:**

| สาเหตุ | วิธีแก้ |
|------|--------|
| Supabase ไม่ตอบสนอง | ตรวจสอบ SUPABASE_URL, SUPABASE_API_KEY |
| Database connection error | ตรวจสอบ .env credentials |
| Network error | ตรวจสอบ internet connection |
| Server ไม่ทำงาน | รันคำสั่ง `npm start` |

**วิธีแก้:**
```bash
# 1. ตรวจสอบ server ทำงาน
curl http://localhost:3000/health
# ✅ ได้ 200 = server ทำงาน
# ❌ Connection refused = server ไม่ทำงาน

# 2. ตรวจสอบ Supabase credentials
# ไปที่ .env แล้วตรวจสอบ:
# - SUPABASE_URL ถูกต้อง
# - SUPABASE_API_KEY ถูกต้อง

# 3. รัน server ใหม่
npm start

# 4. ตรวจสอบ logs
# ดูใน terminal ว่ามี error message ไหม
```

---

## 🌐 Network Errors

### ❌ Connection Refused

**ความหมาย:** ไม่สามารถเชื่อมต่อ localhost:3000

**วิธีแก้:**
```bash
# 1. ตรวจสอบ server ทำงาน
ps aux | grep node
# ✅ ต้องเห็น node process

# 2. รัน server ใหม่
npm start

# 3. ตรวจสอบ port ไม่ถูกใช้ด้วย process อื่น
netstat -ano | findstr :3000
# ❌ ถ้ามี process ใช้ port 3000 ต้องปิดก่อน

# 4. เปลี่ยน port ใน .env
PORT=3001  # ✅ ใช้ port อื่น
```

---

### ❌ Failed to Fetch

**ความหมาย:** Supabase API ไม่ตอบสนอง

**วิธีแก้:**
```bash
# 1. ตรวจสอบ internet connection
ping 8.8.8.8

# 2. ตรวจสอบ SUPABASE_URL ถูกต้อง
# ต้องเป็นแบบ: https://xxxxx.supabase.co
# ไม่ใช่: https://xxx.com

# 3. ตรวจสอบ SUPABASE_API_KEY ถูกต้อง
# ต้องใช้ anon/public key ไม่ใช่ service_role key

# 4. ตรวจสอบ CORS
# Supabase ต้องอนุญาต localhost:3000
```

---

## 🧪 Postman-specific Errors

### ❌ "Variable {{base_url}} is not defined"

**ความหมาย:** Postman ไม่เจอตัวแปร environment

**วิธีแก้:**
```
1. คลิก dropdown ที่มุมบนขวา
2. เลือก "StudentCourse Dev" environment
3. ตรวจสอบตัวแปร base_url มีค่า
```

---

### ❌ "Request timed out"

**ความหมาย:** Server ตอบสนองช้าเกินไป

**วิธีแก้:**
```
1. ตรวจสอบ server ทำงานหรือไม่
   npm start

2. เพิ่ม timeout ใน Postman
   Settings → Postman Runtime → Request timeout

3. ตรวจสอบ Supabase connection
```

---

## ✅ Debugging Checklist

### ✅ ก่อนทดสอบ API

- [ ] Server ทำงาน (`npm start`)
- [ ] Environment variables ถูกต้อง
- [ ] Postman environment เลือกถูก
- [ ] Internet connection ปกติ
- [ ] Supabase credentials ถูกต้อง

### ✅ เมื่อพบปัญหา

- [ ] อ่าน error message อย่างระมัดระวัง
- [ ] ตรวจสอบ request body JSON format
- [ ] ตรวจสอบ HTTP method (GET, POST, PUT, DELETE)
- [ ] ตรวจสอบ URL ถูกต้อง
- [ ] ตรวจสอบ Response status code
- [ ] ดูใน server logs (terminal)

### ✅ ถ้ายังไม่แก้ได้

- [ ] รัน server ใหม่ (`npm start`)
- [ ] Restart Postman
- [ ] ตรวจสอบ .env ใหม่
- [ ] ลองใช้ curl แทน Postman
- [ ] ตรวจสอบ database ใน Supabase console

---

## 🔍 วิธี Debug ด้วย curl

### ✅ Test GET endpoint

```bash
# ดึงนักเรียนทั้งหมด
curl -X GET http://localhost:3000/api/students \
  -H "Content-Type: application/json"
```

---

### ✅ Test POST endpoint

```bash
# สร้างนักเรียนใหม่
curl -X POST http://localhost:3000/api/students \
  -H "Content-Type: application/json" \
  -d '{"fullname":"สมชาย","email":"somchai@example.com","major":"IT"}'
```

---

### ✅ ตรวจสอบ Health Check

```bash
# ตรวจสอบ server ทำงาน
curl http://localhost:3000/health

# ✅ Output:
# {"success":true,"message":"Server is running",...}
```

---

## 📊 Common Error Resolution Table

| Error | Status | สาเหตุ | วิธีแก้ |
|-------|--------|-------|--------|
| Missing fullname | 400 | ไม่ส่ง fullname | ส่ง fullname ใน body |
| Student not found | 404 | ID ไม่มี | ตรวจสอบ ID มีอยู่ |
| Email already exists | 409 | Email ซ้ำ | ใช้ email ใหม่ |
| Connection refused | - | Server ไม่ทำงาน | รัน `npm start` |
| Timeout | - | Server ช้า | ตรวจสอบ internet |
| Invalid JSON | 400 | JSON format ผิด | ตรวจสอบ JSON syntax |
| 500 error | 500 | Database error | ตรวจสอบ credentials |

---

## 🎓 สรุป Error Handling Tips

### ✅ ทำนาย Error

```javascript
// ✅ ทดสอบ success case ก่อน
POST /api/students
{
  "fullname": "สมชาย",
  "email": "somchai@example.com",  // ✅ valid email
  "major": "IT"
}

// ❌ ทดสอบ error case
POST /api/students
{
  "fullname": "",  // ❌ empty
  "email": "invalid",  // ❌ invalid format
  "major": ""  // ❌ empty
}
```

### ✅ อ่าน Error Message

```json
{
  "success": false,
  "message": "⚠️ ขาดข้อมูลที่จำเป็น: fullname, email, major"
  // ✅ Message บอกว่าขาด field ไหน
}
```

### ✅ ใช้ Logs

```bash
# ดู server logs ใน terminal
# จะบอกว่าเกิด error อะไรใน backend
```

---

**สร้างเมื่อ:** 2025-11-30  
**อัปเดตล่าสุด:** 2025-11-30  
**สถานะ:** ✅ เสร็จสมบูรณ์
